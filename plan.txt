1. The user points out two things:
   - "I thought instagram wouldn't be generic". You see, for the "sour thick" entry, the Microlink response was:
     `"image": "https://static.cdninstagram.com/rsrc.php/v4/yD/r/R0fBIMurK8v.png", "title": "Instagram", "description": "Create an account or log in to Instagram..."`.
     This is the generic Instagram login page. Instagram strictly blocks scrapers like Microlink. So Microlink fails to extract any meaningful image/title for raw Instagram profiles.
   - "I thought that we would be downloading the images that microlink.io gives us and save them for references... we have generic code for this". The user points out that we shouldn't hotlink the image URLs (which Microlink returns as raw external URLs), but instead use our `download-images.js` (or similar code) to actually download those files into `images/` locally and refer to the local paths.

2. Problem 1: Microlink on Instagram profiles.
   - Microlink fails for Instagram URLs (`instagram.com/xyz`).
   - Wait, if Microlink fails for Instagram, we shouldn't use it to preview Instagrams!
   - Instead of caching generic Instagram garbage, we should just let the frontend know "We don't have a cache for this, fallback to live Instagram Embed blockquote". Or, for the backend, we skip Microlink entirely if the `url` is `instagram.com`.
   - The user asked to skip Microlink on IG? No, the user said "I thought instagram wouldn't be generic". It means they want real data for Instagram.
   - But wait, we can't scrape Instagram via Microlink without API keys or premium features. Wait, is Microlink actually able to scrape Instagram? Microlink used to work for Instagram but often gets blocked. Alternatively, we can use Linktree or other links as the primary source, or just admit that IG embeds are better for IG links.
   - Wait, in my `js/bear-directory.js` frontend script, I already *do* fall back to the live Instagram Embed if the cache has NO data. If I just make the cache completely ignore Instagram URLs, the frontend will automatically use the live `instgrm.Embeds.process()` for all Instagram URLs! This is the perfect fix.

3. Problem 2: Downloading the images locally.
   - The user wants the microlink images downloaded locally.
   - `tools/download-images.js` handles downloading images for events and bars. It loops through `data/**/*.json`.
   - We should either integrate image downloading into `sync-directory-previews.js` using `tools/download-images.js` functions, or just have `sync-directory-previews.js` write the external URLs to `directory-cache.json`, and then `npm run sync-data` (which runs `download-images.js`) handles it!
   - Wait, `tools/download-images.js` only looks at `data/cities/*.json` and `data/bars/*.json`. It doesn't look at `data/microlink/directory-cache.json`.
   - We need to either update `tools/download-images.js` to parse `directory-cache.json`, OR we just download the image right inside `sync-directory-previews.js`. Given `sync-directory-previews.js` is already fetching data, it's very easy to just download the image and save it to `images/directory/`.
   - Wait, `download-images.js` has a very robust downloader (`downloadFile`). Let's look at it.

Let's check `tools/download-images.js` to see if we can easily add `data/microlink/directory-cache.json` to its sweep, or if it's better to implement it directly in `sync-directory-previews.js`.
